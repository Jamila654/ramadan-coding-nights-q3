chainlit: 2.4.201

ui:
  name: "Advance Agent Gemini"
  description: "Chat with a Gemini Agent"

default_expand_messages: true

auth:
  required: true
  providers:
    - github

oauth_providers:
  github:
    client_id: ${OAUTH_GITHUB_CLIENT_ID}
    client_secret: ${OAUTH_GITHUB_CLIENT_SECRET}